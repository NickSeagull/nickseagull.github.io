---
import Card from "./Card.astro";

import { getCollection, type CollectionEntry } from "astro:content";

const allProjects: CollectionEntry<"projects">[] = (
  await getCollection("projects")
).slice(0, 5);
---

<section id="Projects" class="container mx-auto px-6">
  <div class="overflow-hidden mb-12">
    <div class="flex animate-scroll">
      <div class="flex shrink-0 whitespace-nowrap">
        {
          Array.from({ length: 10 }).map((_) => (
            <h2 class="heading text-5xl md:text-9xl uppercase mr-4 md:mr-8 -mb-2">
              work
            </h2>
          ))
        }
      </div>
      <div class="flex shrink-0 whitespace-nowrap" aria-hidden="true">
        {
          Array.from({ length: 10 }).map((_) => (
            <h2 class="heading text-5xl md:text-9xl uppercase mr-4 md:mr-8 -mb-2">
              work
            </h2>
          ))
        }
      </div>
    </div>
  </div>
  {
    allProjects
      .sort((a, b) => (a.id.substring(0, 2) > b.id.substring(0, 2) ? 1 : -1))
      .map((project) => <Card entry={project} />)
  }
</section>

<style>
  @keyframes scroll {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  .animate-scroll {
    animation: scroll 15s linear infinite;
  }
</style>
